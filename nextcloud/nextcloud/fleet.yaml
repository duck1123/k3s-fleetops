namespace: nextcloud

helm:
  releaseName: nextcloud
  chart: nextcloud
  repo: https://nextcloud.github.io/helm/
  version: 2.6.4
  values:
    nextcloud:
      host: nextcloud.dev.kronkltd.net
    livenessProbe:
      enabled: true
kustomize:
  dir: ./overlays/dev
