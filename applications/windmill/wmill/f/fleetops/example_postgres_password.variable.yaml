value: ""
is_secret: true
description: >-
  Password for example_postgres.resource.yaml. The real value is never stored
  here -- wmill sync push skips secret values by design (see wmill.yaml). It's
  seeded by the windmill-sync ArgoCD hook job from
  env/dev/windmill.nix's `secretVariables` (backed by secrets.enc.yaml).
