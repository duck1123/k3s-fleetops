apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: romm-romm-config
  namespace: romm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
