apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: romm-romm-data
  namespace: romm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
