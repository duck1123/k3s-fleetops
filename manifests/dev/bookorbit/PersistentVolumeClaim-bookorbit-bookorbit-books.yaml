apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookorbit-bookorbit-books
  namespace: bookorbit
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: bookorbit-bookorbit-books-nfs
