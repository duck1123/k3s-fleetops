apiVersion: v1
kind: PersistentVolume
metadata:
  name: bookorbit-bookorbit-audiobooks-nfs
spec:
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 1Ti
  mountOptions:
    - nolock
    - noexec
    - soft
    - timeo=30
  nfs:
    path: /volume1/Audiobooks
    server: 192.168.0.124
  persistentVolumeReclaimPolicy: Retain
