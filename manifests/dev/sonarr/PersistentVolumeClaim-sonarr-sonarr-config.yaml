apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-sonarr-config
  namespace: sonarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
