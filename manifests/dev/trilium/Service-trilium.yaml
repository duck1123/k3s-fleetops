apiVersion: v1
kind: Service
metadata:
  name: trilium
  namespace: trilium
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: trilium
    app.kubernetes.io/name: trilium
  type: ClusterIP
