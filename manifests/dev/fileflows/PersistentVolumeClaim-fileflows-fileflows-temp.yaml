apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fileflows-fileflows-temp
  namespace: fileflows
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: fileflows-fileflows-temp-nfs
