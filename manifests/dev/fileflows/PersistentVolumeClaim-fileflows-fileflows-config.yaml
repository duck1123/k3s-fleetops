apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fileflows-fileflows-config
  namespace: fileflows
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
