apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slskd-slskd-config
  namespace: slskd
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
