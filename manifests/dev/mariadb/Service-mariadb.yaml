apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: mariadb
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: mariadb
    app.kubernetes.io/version: 11.8.4
    helm.sh/chart: mariadb-4.44
  name: mariadb
  namespace: mariadb
spec:
  ports:
    - name: sql
      port: 3306
      protocol: TCP
      targetPort: sql
  selector:
    app.kubernetes.io/instance: mariadb
    app.kubernetes.io/name: mariadb
  type: ClusterIP
