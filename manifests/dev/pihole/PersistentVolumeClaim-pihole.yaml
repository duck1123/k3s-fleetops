apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pihole
    chart: pihole-2.35.0
    component: ""
    heritage: Helm
    release: pihole
  name: pihole
  namespace: pihole
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
