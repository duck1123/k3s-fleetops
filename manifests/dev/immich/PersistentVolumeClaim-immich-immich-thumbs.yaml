apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-immich-thumbs
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: longhorn
