apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-immich-config
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
