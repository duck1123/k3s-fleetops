apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-immich-library
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: immich-immich-library-nfs
