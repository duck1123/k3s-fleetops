apiVersion: v1
kind: Service
metadata:
  name: elasticvue
  namespace: elasticvue
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/name: elasticvue
