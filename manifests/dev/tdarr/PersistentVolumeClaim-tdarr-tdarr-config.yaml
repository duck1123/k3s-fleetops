apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-tdarr-config
  namespace: tdarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
