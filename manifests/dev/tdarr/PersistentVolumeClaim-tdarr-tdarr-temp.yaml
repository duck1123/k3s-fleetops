apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-tdarr-temp
  namespace: tdarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: longhorn
