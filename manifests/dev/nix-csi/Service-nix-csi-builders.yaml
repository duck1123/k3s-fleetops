apiVersion: v1
kind: Service
metadata:
  name: nix-csi-builders
  namespace: nix-csi
spec:
  clusterIP: None
  ports:
    - name: ssh
      port: 22
  selector:
    app.kubernetes.io/name: builder
    app.kubernetes.io/part-of: nix-csi
