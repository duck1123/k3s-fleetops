apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lidarr-lidarr-downloads
  namespace: lidarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: lidarr-lidarr-downloads-nfs
