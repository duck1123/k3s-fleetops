apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lidarr-lidarr-config
  namespace: lidarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
