apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lidarr-lidarr-config
  namespace: lidarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
