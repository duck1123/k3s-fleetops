apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/instance: listenarr
    app.kubernetes.io/name: listenarr
    app.kubernetes.io/version: latest
  name: listenarr
  namespace: listenarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: listenarr
      app.kubernetes.io/name: listenarr
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: listenarr
        app.kubernetes.io/name: listenarr
    spec:
      automountServiceAccountToken: true
      containers:
        - env:
            - name: PGID
              value: "1000"
            - name: PUID
              value: "1000"
            - name: TZ
              value: Etc/UTC
          image: ghcr.io/listenarrs/listenarr:canary
          imagePullPolicy: IfNotPresent
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /ping
              port: 4545
            initialDelaySeconds: 90
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 5
          name: listenarr
          ports:
            - containerPort: 4545
              name: http
              protocol: TCP
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /ping
              port: 4545
            initialDelaySeconds: 60
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 5
          volumeMounts:
            - mountPath: /config
              name: config
            - mountPath: /downloads
              name: downloads
            - mountPath: /podcasts
              name: podcasts
            - mountPath: /audiobooks
              name: audiobooks
      initContainers: []
      serviceAccountName: default
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: listenarr-listenarr-config
        - name: downloads
          persistentVolumeClaim:
            claimName: listenarr-listenarr-downloads
        - name: podcasts
          persistentVolumeClaim:
            claimName: listenarr-listenarr-podcasts
        - name: audiobooks
          persistentVolumeClaim:
            claimName: listenarr-listenarr-audiobooks
