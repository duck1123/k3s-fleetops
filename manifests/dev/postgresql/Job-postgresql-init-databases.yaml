apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
  name: postgresql-init-databases
  namespace: postgresql
spec:
  template:
    spec:
      containers:
        - command:
            - sh
            - -c
            - |+
              set -e
              echo "Creating database prowlarr-main and user prowlarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'prowlarr-main') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'prowlarr-main')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'prowlarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'prowlarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'prowlarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'prowlarr-main', 'prowlarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'prowlarr-main', 'prowlarr');
                END
                \$\$;
              EOSQL
              echo "Database prowlarr-main created successfully"

              echo "Creating database prowlarr-log and user prowlarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'prowlarr-log') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'prowlarr-log')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'prowlarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'prowlarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'prowlarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'prowlarr-log', 'prowlarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'prowlarr-log', 'prowlarr');
                END
                \$\$;
              EOSQL
              echo "Database prowlarr-log created successfully"

              echo "Creating database sonarr and user sonarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'sonarr') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'sonarr')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'sonarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'sonarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'sonarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'sonarr', 'sonarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'sonarr', 'sonarr');
                END
                \$\$;
              EOSQL
              echo "Database sonarr created successfully"

              echo "Creating database sonarr-log and user sonarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'sonarr-log') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'sonarr-log')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'sonarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'sonarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'sonarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'sonarr-log', 'sonarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'sonarr-log', 'sonarr');
                END
                \$\$;
              EOSQL
              echo "Database sonarr-log created successfully"

              echo "Creating database radarr and user radarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'radarr') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'radarr')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'radarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'radarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'radarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'radarr', 'radarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'radarr', 'radarr');
                END
                \$\$;
              EOSQL
              echo "Database radarr created successfully"

              echo "Creating database radarr-log and user radarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'radarr-log') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'radarr-log')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'radarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'radarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'radarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'radarr-log', 'radarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'radarr-log', 'radarr');
                END
                \$\$;
              EOSQL
              echo "Database radarr-log created successfully"

              echo "Creating database lidarr and user lidarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'lidarr') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'lidarr')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'lidarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'lidarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'lidarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'lidarr', 'lidarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'lidarr', 'lidarr');
                END
                \$\$;
              EOSQL
              echo "Database lidarr created successfully"

              echo "Creating database lidarr-log and user lidarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'lidarr-log') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'lidarr-log')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'lidarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'lidarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'lidarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'lidarr-log', 'lidarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'lidarr-log', 'lidarr');
                END
                \$\$;
              EOSQL
              echo "Database lidarr-log created successfully"

              echo "Creating database whisparr and user whisparr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'whisparr') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'whisparr')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'whisparr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'whisparr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'whisparr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'whisparr', 'whisparr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'whisparr', 'whisparr');
                END
                \$\$;
              EOSQL
              echo "Database whisparr created successfully"

              echo "Creating database whisparr-log and user whisparr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'whisparr-log') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'whisparr-log')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'whisparr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'whisparr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'whisparr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'whisparr-log', 'whisparr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'whisparr-log', 'whisparr');
                END
                \$\$;
              EOSQL
              echo "Database whisparr-log created successfully"

              echo "Creating database listenarr and user listenarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'listenarr') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'listenarr')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'listenarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'listenarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'listenarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'listenarr', 'listenarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'listenarr', 'listenarr');
                END
                \$\$;
              EOSQL
              echo "Database listenarr created successfully"

              echo "Creating database listenarr-log and user listenarr..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'listenarr-log') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'listenarr-log')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'listenarr') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'listenarr', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'listenarr', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'listenarr-log', 'listenarr');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'listenarr-log', 'listenarr');
                END
                \$\$;
              EOSQL
              echo "Database listenarr-log created successfully"

              echo "Creating database immich and user immich..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'immich') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'immich')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'immich') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'immich', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'immich', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'immich', 'immich');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'immich', 'immich');
                END
                \$\$;
              EOSQL
              echo "Database immich created successfully"

              echo "Creating database gitea and user postgres..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'gitea') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'gitea')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'postgres') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'postgres', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'postgres', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'gitea', 'postgres');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'gitea', 'postgres');
                END
                \$\$;
              EOSQL
              echo "Database gitea created successfully"

              echo "Creating database affine and user affine..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'affine') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'affine')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'affine') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'affine', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'affine', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'affine', 'affine');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'affine', 'affine');
                END
                \$\$;
              EOSQL
              echo "Database affine created successfully"

              echo "Creating database memos and user postgres..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'memos') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'memos')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'postgres') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'postgres', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'postgres', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'memos', 'postgres');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'memos', 'postgres');
                END
                \$\$;
              EOSQL
              echo "Database memos created successfully"

              echo "Creating database nocodb and user nocodb..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'nocodb') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'nocodb')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'nocodb') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'nocodb', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'nocodb', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'nocodb', 'nocodb');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'nocodb', 'nocodb');
                END
                \$\$;
              EOSQL
              echo "Database nocodb created successfully"

              echo "Creating database romm and user postgres..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'romm') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'romm')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'postgres') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'postgres', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'postgres', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'romm', 'postgres');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'romm', 'postgres');
                END
                \$\$;
              EOSQL
              echo "Database romm created successfully"

              echo "Creating database nostrarchives and user nostrarchives..."
              psql -v ON_ERROR_STOP=1 <<-EOSQL
                -- Create database if it doesn't exist
                SELECT format('CREATE DATABASE %I', 'nostrarchives') WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'nostrarchives')\gexec

                -- Create user if it doesn't exist, then always update password
                DO \$\$
                BEGIN
                  IF NOT EXISTS (SELECT FROM pg_user WHERE usename = 'nostrarchives') THEN
                    EXECUTE format('CREATE USER %I WITH PASSWORD %L', 'nostrarchives', 'omE.6H):+]qedMo>ZG;m');
                  ELSE
                    EXECUTE format('ALTER USER %I WITH PASSWORD %L', 'nostrarchives', 'omE.6H):+]qedMo>ZG;m');
                  END IF;
                END
                \$\$;

                -- Grant privileges (quote database name)
                DO \$\$
                BEGIN
                  EXECUTE format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', 'nostrarchives', 'nostrarchives');
                  EXECUTE format('ALTER DATABASE %I OWNER TO %I', 'nostrarchives', 'nostrarchives');
                END
                \$\$;
              EOSQL
              echo "Database nostrarchives created successfully"

          env:
            - name: PGHOST
              value: postgresql.postgresql
            - name: PGPORT
              value: "5432"
            - name: PGUSER
              value: postgres
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  key: adminPassword
                  name: postgresql-password
          image: pgvector/pgvector:pg17
          imagePullPolicy: IfNotPresent
          name: init-databases
      restartPolicy: OnFailure
