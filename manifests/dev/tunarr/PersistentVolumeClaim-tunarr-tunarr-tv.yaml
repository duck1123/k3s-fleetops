apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tunarr-tunarr-tv
  namespace: tunarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: tunarr-tunarr-tv-nfs
