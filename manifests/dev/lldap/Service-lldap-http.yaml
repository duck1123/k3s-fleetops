apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: lldap
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: lldap
    app.kubernetes.io/version: v0.6.1
    helm.sh/chart: lldap-0.4.2
  name: lldap-http
  namespace: lldap
spec:
  ports:
    - name: http
      port: 17170
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: lldap
    app.kubernetes.io/name: lldap
  type: ClusterIP
