apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stashapp
  namespace: stashapp
spec:
  ingressClassName: traefik
  rules:
    - host: stashapp.home.kronkltd.net
      http:
        paths:
          - backend:
              service:
                name: stashapp
                port:
                  name: http
            path: /
            pathType: ImplementationSpecific
  tls:
    - hosts:
        - stashapp.home.kronkltd.net
