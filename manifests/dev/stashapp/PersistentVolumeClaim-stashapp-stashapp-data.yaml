apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stashapp-stashapp-data
  namespace: stashapp
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: stashapp-stashapp-data-nfs
