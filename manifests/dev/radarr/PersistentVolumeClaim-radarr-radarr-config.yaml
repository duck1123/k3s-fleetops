apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-radarr-config
  namespace: radarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
