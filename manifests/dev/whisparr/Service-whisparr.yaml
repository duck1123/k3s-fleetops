apiVersion: v1
kind: Service
metadata:
  name: whisparr
  namespace: whisparr
spec:
  ports:
    - name: http
      port: 9696
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: whisparr
    app.kubernetes.io/name: whisparr
  type: ClusterIP
