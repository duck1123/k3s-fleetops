apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whisparr-whisparr-gluetun
  namespace: whisparr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
