apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whisparr-whisparr-config
  namespace: whisparr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
