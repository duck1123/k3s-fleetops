apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kapowarr-kapowarr-library
  namespace: kapowarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: kapowarr-kapowarr-library-nfs
