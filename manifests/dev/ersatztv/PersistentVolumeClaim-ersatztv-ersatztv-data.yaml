apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ersatztv-ersatztv-data
  namespace: ersatztv
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
