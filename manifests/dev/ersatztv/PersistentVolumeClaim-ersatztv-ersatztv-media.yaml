apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ersatztv-ersatztv-media
  namespace: ersatztv
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: ersatztv-ersatztv-media-nfs
