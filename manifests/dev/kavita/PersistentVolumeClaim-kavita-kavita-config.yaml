apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-kavita-config
  namespace: kavita
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
